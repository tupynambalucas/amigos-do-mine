name: amigos-portal

services:
  portal-db:
    build:
      context: ./infrastructure/mongodb
      dockerfile: Dockerfile
    container_name: amigos-portal-db
    restart: always
    ports:
      - '27017:27017'
    command: ['mongod', '--replSet', 'rs0', '--bind_ip_all', '--keyFile', '/data/mongo-keyfile']
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_DB_NAME}
    volumes:
      - amigos-portal-mongo-data:/data/db
    networks:
      - amigos-portal-network
    healthcheck:
      test: ['CMD', 'mongosh', '--eval', "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5

  portal-db-seed:
    build:
      context: ./infrastructure/mongodb
      dockerfile: Dockerfile
    container_name: amigos-portal-db-seed
    # Sobrescreve o entrypoint para rodar o script de seed
    entrypoint: ["./seed.sh"]
    depends_on:
      portal-db:
        condition: service_healthy
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_DB_NAME}
      ADMIN_USER_SEED: ${ADMIN_USER_SEED}
      ADMIN_EMAIL_SEED: ${ADMIN_EMAIL_SEED}
      ADMIN_PASS_SEED: ${ADMIN_PASS_SEED}
    networks:
      - amigos-portal-network

  portal-redis:
    build:
      context: ./infrastructure/redis
      dockerfile: Dockerfile
    container_name: amigos-portal-redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - amigos-portal-redis-data:/data
    networks:
      - amigos-portal-network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  amigos-portal-network:
    driver: bridge

volumes:
  amigos-portal-mongo-data:
  amigos-portal-redis-data:
